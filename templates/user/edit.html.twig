{% extends 'base.html.twig' %}

{% block title %}Edit User
{% endblock %}

{% block body %}
	<header class="header-edit-form">
		<nav class="nav-bar-edit nav-bar-svg">			
				<img src="{{ asset('images/star.svg') }}" class="logo logo-edit" alt="Logo Astra" style="max-width: 60px; max-height: 60px;">
				<div>
					<a href="{{ path('app_logout') }}" class="btn">
						<img src="{{ asset('images/box-arrow-right.svg') }}" class="logo" alt="logout icon" style="max-width: 30px; max-height: 30px;">
					</a>
				</div>
			</nav>
	</header>
		<h1>Modifier votre profil</h1>

		{{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

		<a href="{{ path('app_user_index', {'id': app.user.id}) }}">back to profile</a>

		{{ include('user/_delete_form.html.twig') }}
	{% endblock %}
