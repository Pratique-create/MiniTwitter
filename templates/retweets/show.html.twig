{% extends 'base.html.twig' %}

{% block title %}Retweets{% endblock %}

{% block body %}
    <h1>Retweets</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ retweet.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ retweet.createdAt ? retweet.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_retweets_index') }}">back to list</a>

    

    {{ include('retweets/_delete_form.html.twig') }}
{% endblock %}
